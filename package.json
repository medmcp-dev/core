{
  "name": "@medmcp/core",
  "version": "0.1.0",
  "description": "Medical reasoning API for AI agents — structured risk signals from clinical input",
  "type": "module",
  "main": "dist/index.js",
  "files": ["dist/", "src/"],
  "scripts": {
    "build": "tsc",
    "test:analyze": "npm run build && node --test tests/analyze/*.test.mjs",
    "test:all": "npm run test:analyze && npm test --prefix sdk && cd sdk-python && python -m unittest discover -s tests -p \"test_*.py\"",
    "start": "node dist/index.js",
    "start:http": "node dist/http/server.js",
    "dev": "tsc --watch",
    "seed": "node dist/db/seed.js",
    "setup": "npm run build && npm run seed",
    "sync:drugs": "node dist/sync/sync-drugs.js",
    "sync:icd11": "node dist/sync/sync-icd11.js",
    "sync": "npm run sync:drugs && npm run sync:icd11",
    "api-keys": "node dist/scripts/api-keys-admin.js"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.1",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "better-sqlite3": "^12.9.0",
    "dotenv": "^17.4.2",
    "hono": "^4.12.17",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.8",
    "@types/node": "^26.1.1",
    "typescript": "^6.0.3"
  }
}
